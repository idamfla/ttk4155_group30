{
    "name": "AVR-GCC Dev Container",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "github.copilot",
                "github.copilot-chat"
            ]
        }
    },
    "mounts": [
        "source=/dev/bus/usb,target=/dev/bus/usb,type=bind"
    ],
    "runArgs": [
        // "--device=/dev/bus/usb"
        "--privileged"
    ]
}